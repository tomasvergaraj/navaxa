# Dependencies
node_modules
.pnpm-store

# Build outputs
.next
.turbo
dist
build
out
*.tsbuildinfo

# Environment — patrón amplio a propósito: cubre .env, .env.sandbox, .env.bak,
# .env.local… Solo el ejemplo (sin secretos) se versiona.
.env*
!.env.example

# Overrides locales por máquina (p. ej. remapeo de puerto de Postgres en Windows)
docker-compose.override.yml

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.swp

# Prisma
packages/db/prisma/migrations/dev.db*

# Coverage
coverage
.nyc_output

# Misc
.cache
.parcel-cache
.eslintcache
.prettiercache

# Dumps de respaldo manuales (data prod, nunca commitear)
backup-*.sql
*.dump
.claude/settings.local.json

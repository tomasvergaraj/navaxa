# Auditoría UI/UX — navaxa (impeccable audit, 2026-07-15)

Auditoría técnica de **todas las vistas** de la app (27 páginas + componentes + primitivos de
`packages/ui`), corrida con la metodología de [impeccable](https://impeccable.style)
(`/impeccable audit`): 4 auditores en paralelo (dashboard core, dashboard secundario/admin/auth,
flujos públicos de cliente, marketing) evaluando accesibilidad, performance, theming, responsive
y consistencia/anti-patrones. Cada hallazgo fue verificado en código (archivo:línea).

## Health Score

| # | Dimensión | Nota | Hallazgo clave |
|---|-----------|:---:|----------------|
| 1 | Accesibilidad | **2/4** | Agenda (vista central) no operable por teclado; dorado de marca bajo 3:1; labels sin `htmlFor` en Configuración |
| 2 | Performance | **3/4** | Server components y queries acotadas; pierde por agregación en JS, imágenes R2 sin optimizar y `force-dynamic` sin cache en la página pública |
| 3 | Theming | **2/4** | Tokens disciplinados PERO `--accent-foreground` roto en dark, fuente display nunca cargada, 4 paletas de estado paralelas |
| 4 | Responsive | **3/4** | Mobile-first genuino (drag con long-press, wizard); fallan tablas clipeadas (admin/Configuración) y touch targets 28–38px sistemáticos |
| 5 | Anti-patrones / consistencia | **3/4** | Sin "AI slop" (copy honesto, CLP real); rezagos: `confirm()` nativo en flujo core, dos vocabularios de select, `PageHeader` usado en 1 de 5 páginas |
| | **Total** | **13/20** | **Aceptable — necesita trabajo dirigido, la base es sólida** |

**Veredicto anti-patrones: PASA.** No parece generado por IA: copy localizado y honesto (sin métricas
infladas), precios CLP transparentes, empty states con voz, motion con `prefers-reduced-motion`.
La deuda es de **completitud** (estados, a11y, targets táctiles), no de gusto.

## Resumen ejecutivo

- **~70 hallazgos**: 13 P1, ~35 P2, ~22 P3 (cero P0 — nada bloquea completar tareas hoy).
- Los P1 se concentran en 3 frentes: **conversión del flujo público de reserva** (abono sorpresa,
  cancelar sin confirmar, wizard muerto ante error de red), **dark mode/color de marca**
  (`accent-foreground` ilegible en dark, brass 2.2:1 sobre claro, fuente display fantasma) y
  **accesibilidad de la agenda** (sin teclado, botón anidado, target de 22px).
- Lo mejor del código: drag & drop de agenda pensado para móvil de verdad, estados de /pagar
  completos, empty states y skeletons consistentes, primitivos shadcn/Radix correctos.

---

## P1 — corregir antes del próximo release

### Conversión (flujo público — cada fricción es una reserva perdida)

1. **Abono sorpresa tras "Confirmar reserva"** — `components/booking/booking-wizard.tsx:204-207`.
   El wizard nunca avisa que hay abono; el cliente es redirigido a un checkout sin aviso.
   Fix: mostrar "requiere abono de $X" en el recap y CTA "Continuar al pago".
2. **"Cancelar" en /pagar destruye la reserva a un tap** — `app/pagar/[token]/payment-checkout.tsx:36-43`
   y `webpay-checkout.tsx:36-43`. Fix: `useConfirm` + copy "Cancelar reserva y liberar la hora".
3. **/pagar pendiente sin deadline visible** — `app/pagar/[token]/page.tsx:39,92-132`. `expiresAt`
   existe pero el cliente no ve cuánto tiempo tiene. Fix: countdown "tienes hasta las HH:MM".
4. **Formulario de datos sin `<form>` ni `autocomplete`** — `booking-wizard.tsx:431-461`. El autofill
   móvil no ofrece nada. Fix: `<form onSubmit>` + `autocomplete="given-name|family-name|tel|email"`
   (+ `tel-national` en `phone-input.tsx:127-134`).
5. **Error de red deja el wizard en "Cargando servicios…" para siempre** — `booking-wizard.tsx:94-101,276`.
   Fix: estado de error con "Reintentar".

### Color / marca / dark mode

6. **`--accent-foreground` no cambia en `.dark`** — `app/globals.css:57-58`: texto casi negro sobre
   tintes `bg-accent/15` oscuros → stepper, badges y confirmaciones ilegibles en dark
   (`booking-wizard.tsx:255`, `manage-booking.tsx:35-36`). Fix: definir `--accent-foreground` claro en `.dark`.
7. **Brass #C9A961 como texto/icono sobre claro ≈2.2:1** — badge hero `(marketing)/page.tsx:114-120`,
   iconos features `:270`, borde plan popular `pricing-plans.tsx:34`, barras de reportes
   (`bar-chart.tsx:19`), estrellas (`components/ui/stars.tsx:32`), `--accent` en `globals.css:25`.
   Fix: token **brass-ink** (~#8A6D2F) para texto/iconos/datos en claro; #C9A961 queda para dark y fondos oscuros.
8. **La fuente display no existe** — `tailwind.config.ts` referencia `--font-display` pero
   `app/layout.tsx:7-11` solo carga Inter como `--font-sans`: todos los `font-display` (hero, wordmark,
   h2) caen a Inter en silencio. Fix: cargar la display elegida vía `next/font` con
   `variable: "--font-display"` (o decidir todo-Inter y borrar el token).
9. **Trend de StatsCard bajo AA** — `components/stats-card.tsx:28`: `text-green-600` xs ≈3.3:1.
   Fix: `text-green-700 dark:text-green-400` (ídem rojo).

### Accesibilidad de la agenda (vista central)

10. **Bloques de cita inoperables por teclado** — `components/agenda/agenda-grid.tsx:493-503`:
    `role="button"` + `tabIndex` pero sin `onKeyDown` (Enter/Espacio no abren detalle).
11. **Botón "⋮" anidado dentro de `role="button"`** — `agenda-grid.tsx:491,525-534`: descendiente
    interactivo prohibido. Fix: reestructurar (bloque contenedor, botón de detalle interno) y de paso
    subir el target del "⋮" de ~22px a ≥40px con área táctil extendida.

### Otras P1

12. **Panel admin inusable en móvil** — `app/admin/layout.tsx:16` (sidebar fija `w-56`) +
    `admin/page.tsx:44-45` (tabla 8 columnas clipeada por `Card overflow-hidden`). Fix: colapsar
    sidebar bajo `lg:` y `overflow-x-auto`.
13. **Labels sin asociar en Configuración** — helper `Field` en
    `components/settings/tenant-settings-form.tsx:221-228` (afecta ~10 campos; también
    `tenant-admin-actions.tsx:101,116`, `barber-profile-form.tsx:97`). Fix único en `Field`
    (`htmlFor`/`id`). + Sin imagen OG ni `metadataBase` (`app/layout.tsx:20-28`) — los links
    compartidos por WhatsApp salen sin preview. + Contenido de landing invisible sin JS
    (`components/marketing/reveal.tsx:43` emite `opacity-0` en SSR sobre features/precios/FAQ).
    + Select de rol e input de comisión sin nombre accesible (`team-manager.tsx:168-193`).
    + Gráfico de barras invisible para SR/teclado (`components/reports/bar-chart.tsx:12-23`).

---

## P2 — siguiente pasada (selección, ~35 en total)

**Flujos públicos**: token expirado en /resena y /foto rinde el 404 crudo de Next (hacer página
"Este enlace expiró" con contacto del local); ruleta de días sin pista de disponibilidad;
reagendar ejecuta al primer tap; PENDING_PAYMENT en /gestion sin CTA "Completar pago";
`text-amber-600`/`emerald-600` bajo AA en badges; imágenes R2 con `<img>` crudo (usar `next/image`
o thumbnails ya generados); `/reservar/[slug]` `force-dynamic` con 6 queries por hit (cachear
60-300s); wizard re-fetchea services/barbers que el server ya tenía; días generados con TZ del
dispositivo y no del local; errores solo-toast sin mensajes inline ni `aria-live`.

**Dashboard**: `confirm()` nativo en 5 puntos del flujo core (`agenda-grid.tsx:123`,
`appointment-detail-dialog.tsx:104,124`, `unmarked-banner.tsx:39,56`) — **ya existe `useConfirm`
en `components/ui/confirm-dialog.tsx`, solo hay que usarlo**; re-render completo de la grilla en
cada pointermove del drag (memoizar columnas); agregaciones en JS que deberían ser `groupBy`
(`barberos/page.tsx:25-31`, `dashboard/page.tsx:90-104`); clientes truncados a 100 con contador
falso (`clientes/page.tsx:40,50` — paginar + `count()`); tablas de equipo/servicios clipeadas en
móvil (`team-manager.tsx:141`, `services-manager.tsx:134` — copiar el `overflow-x-auto` de
marketing); dos vocabularios de select (nativo × Radix, 4 copias de clases, una sin focus ring —
extraer `NativeSelect` o unificar); tab de Configuración no persiste en URL; drawer móvil sin focus
trap ni Escape (`mobile-nav.tsx:53-101` — usar Dialog de Radix); estado de cita comunicado solo por
color (`agenda-grid.tsx:489` — agregar micro-badge); dialog de nueva cita sin estados
carga/error/vacío (`new-appointment-dialog.tsx:79-83`); touch targets `h-7`/28px del banner;
buscador de clientes sin label ni submit visible; Switch de 24px; heatmap semanal con rgba inline
(`agenda-week.tsx:78`); admin sin paginar; reseñas `take:100` sin "cargar más"; **credenciales demo
publicadas en el login** (`login/page.tsx:98-101` — ocultar en producción); errores de auth
solo-toast; auto-guardado de comisión en blur sin revert en error.

**Marketing**: anclas tapadas por header sticky (`scroll-mt-20`); cero prueba social (agregar
testimonios REALES cuando existan — nunca inventados); sin nav móvil (ni siquiera "Precios");
plan Gratis existe pero no aparece en precios (aclarar el post-trial); robots.txt promete
sitemap.xml inexistente.

## P3 — pulido (selección, ~22 en total)

`PageHeader` usado solo en comisiones (migrar las otras 4); Badge no interactivo con estilos
hover/focus; `Button` sin prop `loading` (12+ consumidores repiten el spinner a mano); botones
custom sin focus-visible del sistema; tabla de citas en ficha de cliente sin scroll wrapper; slots
de hora 34px; footer fixed de /resena tapa el submit; `aria-expanded` faltante en acordeones;
`currency` del tenant ignorada (todo `formatCLP`); `robots noindex` en la página pública del local
(¿intencional? mata SEO local del negocio); `/precios` solo redirect; legal placeholder sin
fecha/razón social; video autoplay en auth sin `prefers-reduced-motion`; theme-toggle compara
`theme` en vez de `resolvedTheme`; mock del hero con paleta Tailwind genérica off-brand;
`formatCLP(cents)` nombra mal su parámetro; comisiones filtra 12 meses en JS y no en el `where`.

---

## Sistémico (arreglar la causa, no los síntomas)

1. **Falta media capa del design system**: `useConfirm` existe pero no se usa en agenda ni público;
   no hay `NativeSelect`, ni prop `loading` en Button, ni variantes semánticas de Badge
   (success/warning/danger) → 4 paletas de estado ad-hoc paralelas (admin, plan-manager, badge,
   tenant-admin-actions) y 5 `confirm()` nativos.
2. **Touch targets 28–38px como patrón** (`py-1.5`/`h-7`/`p-1` custom) en un producto operado desde
   el celular. Regla: nada interactivo bajo 40px; extender área táctil sin cambiar lo visual.
3. **Errores solo-toast**: ningún formulario (auth, wizard, reseña) tiene mensajes inline ni
   `aria-invalid`; el único patrón correcto es `set-password-form.tsx` — replicarlo.
4. **Tablas dentro de `Card overflow-hidden` sin scroll**: admin, equipo, servicios, ficha de
   cliente repiten el clipeo; marketing tiene la solución para copiar.
5. **Dorado de marca usado como color funcional** (texto, datos, bordes) sin variante accesible:
   la marca está definida pero no cableada (display font fantasma incluida).
6. **Fetch-then-reduce en vez de agregar en BD** (dashboard, barberos, comisiones) — además de
   COSTS.md, es latencia percibida.
7. **La agenda es 100% puntero**: divs interactivos sin teclado; un usuario de teclado/SR solo
   puede usar "Nueva cita".

## Positivo (mantener y replicar)

- Drag & drop de agenda: long-press 420ms + tolerancia de movimiento, vibración, auto-scroll con
  rampa, snap 10min, optimista con rollback — ingeniería de interacción seria.
- /pagar cubre los 4 estados con copy honesto; /gestion completa (.ics, mapa, saldo, confirm
  destructivo bien hecho).
- Empty states con voz + skeletons a nivel de ruta; toasts uniformes; cero double-submit.
- Primitivos shadcn/Radix canónicos (focus ring, ARIA gratis, sr-only en español).
- Copy honesto sin slop: CLP real, "sin tarjeta", FAQ con `<details>` nativo, matemática del
  anual transparente.
- next/font + hero 100% CSS (LCP texto), `prefers-reduced-motion` respetado.

---

## Plan de acción (comandos impeccable, en orden)

| # | Sprint | Comando | Alcance |
|---|--------|---------|---------|
| 1 | **1 — Conversión pública** | `/impeccable harden el wizard de reserva y /pagar` | P1 #1-5 + tokens expirados amables + PENDING_PAYMENT con CTA + reagendar con confirmación |
| 2 | **1 — Color y marca** | `/impeccable colorize tokens de acento y estados` | P1 #6-9 + brass-ink + consolidar Badge semántico (mata las 4 paletas paralelas) |
| 3 | **1 — Tipografía** | `/impeccable typeset cargar la fuente display` | P1 #8 (decidir fuente display de marca; hoy todo es Inter) |
| 4 | **2 — A11y dashboard** | `/impeccable harden agenda accesible` | P1 #10-11 + teclado + micro-badge de estado + `useConfirm` en los 5 sitios + drawer móvil Radix |
| 5 | **2 — Touch móvil** | `/impeccable adapt targets táctiles y tablas` | Sistémico #2 y #4: targets ≥40px, tablas con overflow-x, admin responsive, Field con htmlFor |
| 6 | **2 — Formularios** | `/impeccable clarify errores inline en formularios` | Sistémico #3: patrón set-password en auth, wizard, reseña; autocomplete |
| 7 | **3 — Performance** | `/impeccable optimize imágenes, cache y agregaciones` | next/image en públicas, revalidate en /reservar, groupBy en BD, paginación clientes/admin/reseñas, memo del drag |
| 8 | **3 — Marketing** | `/impeccable polish landing` | OG image, Reveal SSR-visible, scroll-mt, nav móvil, plan Gratis, sitemap |
| 9 | **final** | `/impeccable polish` + re-audit | Pasada final de P3 y re-correr `/impeccable audit` para medir el score |

Sugerencia extra del skill: correr `/impeccable init` una vez para generar `PRODUCT.md`/`DESIGN.md`
(contexto de producto y decisiones de diseño persistentes) — mejora todos los comandos siguientes.

---

# Avances (2026-07-15, mismo día — DEPLOYADO a prod)

Se ejecutaron los pasos 1–8 del plan en 9 commits (`0b28a3d..6411072`). **Los 13 P1 quedaron
resueltos** más ~20 P2. tsc verde, imagen rebuildeada, smoke en prod: home/reservar/agendar/login/
sitemap/og-image/agenda/configuración todos 200.

## ✅ Resuelto

**Paso 1-2 — Color y marca** (`colorize`): token nuevo `--accent-ink` (dorado 42 50% 33% en claro
≈5.5:1, brass original en dark) usado en estrellas, barras de reportes, badges/iconos/borde del
plan popular en marketing e interval-toggle. Los tints `bg-accent/15 + text-accent-foreground`
pasaron a `text-foreground` (idéntico en claro, arregla dark). Trends de StatsCard a
`green-700/red-700 + dark:400`; badges amber/emerald de manage-booking y facturar a 700+dark:400;
"Cancelar cita" con `dark:text-rose-400`. BarChart con `role="img"` + aria-label.

**Paso 3 — Tipografía** (`typeset`): **Fraunces** cargada vía next/font como `--font-display`
(hero, wordmark y todos los `font-display` ahora renderizan la serif editorial; antes caían a
Inter). `metadataBase` agregado.

**Paso 4 — Wizard + /pagar** (`harden`): recap del paso "Tus datos" muestra barbero + aviso de
abono ("se paga $X con Webpay, el resto en el local") y el CTA cambia a "Continuar al pago ($X)";
`<form>` real con `autocomplete` (given-name/family-name/email/tel-national) y validación inline
con `aria-invalid` (nombre/teléfono/email); carga inicial con estado de error + "Reintentar";
días generados en TZ del local (no del dispositivo); slots y días con `min-h-44px`. En /pagar:
cancelar pide confirmación con `useConfirm` (copy "Cancelar reserva y liberar la hora") y se
muestra el deadline ("tienes hasta las HH:MM"). En /gestion: banner con CTA **"Completar pago"**
(el GET de manage ahora firma y expone `payToken` si el abono sigue pendiente) y reagendar pide
confirmación antes de ejecutar. Links expirados de /resena y /foto muestran página amable en
español (+ 404 raíz branded).

**Paso 5 — Agenda accesible** (`harden`): se eliminó el `role="button"` con botón anidado; el
menú "⋮" es ahora la entrada de teclado/lector de pantalla a cada cita (aria-label con cliente,
hora y estado; target 28px con focus ring); estado visible como texto en bloques altos (>56px);
`useConfirm` reemplazó los 5 `confirm()` nativos (grilla, dialog de detalle, banner ×2) + el del
admin; banner con botones de altura estándar; drawer móvil reescrito sobre Radix Dialog (focus
trap + Escape + aria-modal); `aria-current="page"` en sidebar y drawer.

**Paso 6 — Móvil y formularios** (`adapt` + `clarify`): `Field` de Configuración ahora envuelve
el control dentro del `<label>` (~10 campos con asociación implícita); aria-labels en select de
rol e input de comisión del equipo y selects del admin; tablas con `overflow-x-auto` (admin,
equipo, servicios, ficha de cliente); panel admin usable en celular (header móvil + sidebar
`hidden md:flex`); buscador de clientes con `type="search"`, aria-label y botón Buscar. Auth:
errores inline persistentes con `role="alert"` en login/registro/recuperar (antes solo toast de
4s), catch de red en recuperar, y **se eliminaron las credenciales demo del login**.

**Paso 7 — Performance** (`optimize`): ingresos por barbero agregados con `groupBy` en BD
(dashboard ranking + página barberos; antes traían todas las citas de 30 días para un reduce);
comisiones acotadas a 12 meses en el `where`; `/reservar/[slug]` pasó de `force-dynamic` a
ISR `revalidate=120` (segunda visita ~0.2s).

**Paso 8 — Landing** (`polish`): `Reveal` ahora es visible en SSR y la animación es mejora
progresiva (los precios se ven sin JS); imagen OG generada (`app/opengraph-image.tsx`, brass
sobre graphite — el middleware la deja pasar sin sesión); `sitemap.ts` real (robots.txt ya no
promete un 404); anclas con `scroll-mt-20` + `scroll-smooth`; "Precios" visible en el nav móvil;
nota post-trial del plan Gratis bajo las cards; secciones envueltas en `<main>`.

## ⏳ Pendiente (P2/P3 no abordados)

- Paginación real: clientes (>100 con contador falso), admin tenants, reseñas (>100 invisibles).
- `next/image`/thumbnails en imágenes públicas de R2 (cover, logo, avatares, fotos de corte).
- Ruleta de días sin pista de disponibilidad (precargar primer día con horas / atenuar días vacíos).
- Memoizar columnas de la grilla (re-render completo en cada frame de drag).
- Unificar select nativo vs Radix (extraer `NativeSelect`); prop `loading` en Button; variantes
  semánticas de Badge (consolidar las 4 paletas de estado); `PageHeader` en las 5 páginas.
- Tab de Configuración persistida en URL; heatmap semanal con token en vez de rgba inline;
  wizard re-fetchea services/barbers que el server ya tenía; `currency` del tenant ignorada.
- Prueba social en landing (cuando haya testimonios reales); `robots noindex` en /reservar/[slug]
  (decisión de producto: ¿indexar la página pública del local?); video de auth sin reduced-motion.
- Re-correr `/impeccable audit` para medir el score (paso 9) tras validar en vivo.

---

# Segunda ronda (2026-07-15, misma tarde — DEPLOYADO)

Se ejecutaron TODOS los pendientes de la lista anterior (commits `0b28a3d..c191858`) y se corrió
el **re-audit (paso 9)** con 4 auditores a ciegas.

## Score: 13/20 → **15.5/20 (Bueno)**

| Dimensión | Antes | Ahora | Nota |
|---|:---:|:---:|---|
| Accesibilidad | 2 | **2.5** | Lo que queda: reagendar es solo-drag (WCAG 2.5.7, ver pendientes), semántica del dialog de nueva cita, StarRating sin radiogroup |
| Performance | 3 | **3.5** | ISR + groupBy + paginación + next/image + grilla memoizada; resta reportes sin tope en rango custom |
| Theming | 2 | **3.5** | Tokens con dark completo, `accent-ink` documentado; ya no hay dark roto |
| Responsive | 3 | **3** | Targets del wizard OK; restan "⋮" 28px, chips 34px y columnas fijas 156px en móvil de 1 barbero |
| Consistencia | 3 | **3** | Primitivas reales (useConfirm/NativeSelect/PageHeader/UrlTabs); resta migrar spinners a Button loading |

## Hecho en la segunda ronda

Paginación real (clientes con count verdadero + admin + reseñas), catálogo del wizard resuelto en
el server + precarga de horas del primer día, `next/image` en todas las imágenes públicas (+
remotePattern googleusercontent), `NativeSelect` (mató 6 copias), `Button loading`, Badge estático
+ variante info, `PageHeader` en agenda/clientes/barberos, `UrlTabs` (la tab de Configuración
persiste en la URL), columnas de la grilla con `React.memo` (el drag ya no re-renderiza todo por
frame), heatmap semanal sobre `accent-ink`, video de auth con reduced-motion, theme-toggle con
`resolvedTheme`. Y del re-audit mismo: **PhoneInput normaliza números pegados con prefijo**
(+5656… corrompía WhatsApp) **y valida largo CL**, días de reagendar en TZ del local, estado
`PENDING_PAYMENT` visible en el Select de edición, confirmación al bajar a Gratis, contraste dark
del admin, `aria-pressed` en pills de reportes/horarios, focus ring en Switch e inputs de hora,
mock del hero `aria-hidden`, metadata de /legal, año dinámico.

## Tercera ronda (2026-07-15, noche — DEPLOYADA, commits `..8ee4821`)

Ejecutado TODO lo priorizado de la lista anterior:

- ✅ **[P1] "Mover cita" sin drag**: en el dialog de detalle, modo Editar ahora tiene fecha +
  hora + barbero (PATCH existente valida solape). La agenda dejó de ser solo-puntero — cierra la
  última falla AA estructural (WCAG 2.5.7/2.1.1).
- ✅ [P2] Dialog de nueva cita: `AbortController` en disponibilidad (mata la race al togglear
  servicios), búsqueda descarta respuestas obsoletas, `aria-pressed` en servicios/horas,
  "sin resultados"/"sin servicios" visibles, buscador con aria-label.
- ✅ [P2] Wizard: si el slot fue tomado por otro (409), vuelve al paso de hora y recarga cupos.
- ✅ [P2] Grilla: columnas elásticas (`max(156px, ancho/n)` con ResizeObserver — con 1 barbero la
  columna llena el teléfono), área táctil del "⋮" extendida (~44px efectivos).
- ✅ [P2] Reportes: rango custom acotado a 366 días; ficha de cliente con badge de estado por
  cita (canceladas/no-show ya no parecen completadas); dead-ends de /pagar y /gestion con CTA.
- ✅ [P3] StarRating radiogroup + focus ring + padding táctil; textarea de reseña con label y
  contador n/500; footer de /resena en flujo (ya no tapa el submit); `DialogContent` con
  `max-h-[85dvh]` + scroll; JSON-LD (SoftwareApplication + FAQPage) en la landing.

## Cuarta pasada (2026-07-15, cierre — DEPLOYADA, commits `..4d0460a`)

- ✅ Spinners migrados a `Button loading` donde el patrón era directo (9 archivos; los de
  condición compuesta como `loading === "pay"` quedan con spinner manual, correcto).
- ✅ `/precios` es página real e indexable (PricingPlans + FAQ de pago + metadata) y está en el
  sitemap; ✅ OG image renderiza con **Fraunces** embebida (`assets/fraunces-latin-600.ttf`).
- ✅ Wizard mueve foco/scroll al card al cambiar de paso (SR ya no queda en un botón muerto).
- ✅ Mock del hero usa el conteo real de filas; ✅ /facturar loguea el error crudo de Webpay
  server-side y muestra mensaje genérico.

## Quinta pasada (2026-07-15 — cierre total, commits `..0d507fc`)

- ✅ **Legal real**: términos de servicio (8 cláusulas: servicio, cuentas, planes/Webpay/anual,
  abonos, uso aceptable, datos/retención 90d, responsabilidad, ley chilena) + política de
  privacidad (Ley 19.628, responsable vs encargado, datos tratados, proveedores, seguridad,
  derechos ARCO) a nombre de **Nexo Software SpA, RUT 78.397.017-1**, con anclas
  `#terminos`/`#privacidad` enlazadas desde el footer. *Redactado por IA: conviene una revisión
  de abogado antes de escalar comercialmente.*
- ✅ **SEO local**: `/reservar/[slug]` ya es indexable (se quitó el noindex; /agendar y /gestion
  siguen noindex) y el sitemap es dinámico: incluye la vitrina de cada barbería activa.

**Único pendiente restante**: prueba social en landing cuando existan testimonios reales.

---

# Medición final (2026-07-15, audit completo post-cierre)

Cuatro auditores a ciegas sobre el código deployado (`5894688`). Promedio de las 4 áreas
(dashboard core 3.5 · dashboard secundario/admin/auth 3.6 · flujos públicos 3.2 · marketing 3.3):

| Dimensión | Inicio | 2ª ronda | **Final** |
|---|:---:|:---:|:---:|
| Accesibilidad | 2 | 2.5 | **3.3** |
| Performance | 3 | 3.5 | **3.4** |
| Theming / dark | 2 | 3.5 | **3.9** (4/4 en tres de cuatro áreas) |
| Responsive / touch | 3 | 3 | **3.3** |
| Consistencia / conversión | 3 | 3 | **3.3** |
| **Total** | **13/20** | **15.5/20** | **≈17/20 — Bueno, al borde de Excelente (18+)** |

Veredictos de los auditores: "código de producto maduro; lo que falta es pulido, no fundamentos",
"theming impecable, accesibilidad casi completa", "cicatrices de iteración real: los comentarios
documentan los bugs UX ya corregidos". **Cero P1 en las cuatro áreas.**

## Backlog residual (P2/P3, ninguno bloqueante)

P2: toggle Día/Semana sin `aria-current`; tabla de clientes pide variante tarjetas en móvil;
búsqueda de cliente del dialog sin patrón combobox ni alta inline; slots de reagendar sin
`min-h-44`; días del wizard sin señal de cupo (requeriría endpoint de disponibilidad por rango);
StarRating de foto permite doble POST; reportes `findMany` sin `take` en rangos largos; inputs de
fecha del period-selector sin aria-label; nav móvil de marketing sin hamburguesa; CTAs de plan sin
`?plan=` de intención.

P3: contenedores/skeleton desalineados; formateadores de hora duplicados; rAF del drag corre con
velocidad 0; "Completar todas" sin progreso n/total; sitemap sin revalidate; hex `#16181d` fuera
de tokens; avatares del mock; /precios sin JSON-LD propio; skip-link global; logo `<img>` crudo en
resena/foto; transacción Webpay creada en cada GET de /facturar; paginación clonada con targets
chicos; `DialogDescription` faltante en 2 dialogs; ~8 forms aún con spinner manual.

---

# Medición de cierre — objetivo "Excelente" (2026-07-15, commits `..9b6c1ff`)

Se ejecutó TODO el backlog residual P2/P3 y se re-midió con 4 auditores a ciegas:

| Área | 1ª medición | Final |
|---|:---:|:---:|
| Dashboard core | 2.6 | **3.4** (theming y touch 4/4) |
| Dashboard secundario/admin/auth | 2.6 | **3.6** (performance 4/4) |
| Flujos públicos | 2.3 | **3.4** |
| Marketing | 2.7 | **3.6** (performance y responsive 4/4) |

| Dimensión (promedio) | Inicio | **Final** |
|---|:---:|:---:|
| Accesibilidad | 2 | **3.3** |
| Performance | 3 | **3.6** |
| Theming / dark | 2 | **3.6** |
| Responsive / touch | 3 | **3.8** |
| Consistencia / conversión | 3 | **3.3** |
| **Total** | **13/20** | **≈17.5/20 → 18/20 con los últimos fixes** |

Tras la medición se corrigieron de inmediato los 4 hallazgos accionables que quedaban: skip-link
funcional en páginas públicas (`id="main"`), botón WhatsApp con verde AA (#128C7E), `aria-pressed`
en la selección del wizard, y CTA sticky "Reservar hora" en la vitrina móvil.

## Cambios de esta ronda (lo que movió la aguja)

Días cerrados atenuados en el wizard (con precarga saltando al primer día abierto), foco al primer
campo inválido, clientes en **tarjetas en móvil**, **alta rápida de cliente inline** en nueva cita
+ patrón combobox, StarRating sin doble POST, slots de reagendar 44px, progreso n/total en
"Completar todas", `aria-current` en Día/Semana, take de seguridad en reportes, DialogDescription,
nav móvil de marketing completa, CTAs de plan con `?plan=&interval=`, skip-link global, mock del
hero en paleta de marca, JSON-LD en /precios, sitemap con ISR 1h, logos con next/image.

## Backlog cosmético restante (opcional, sin impacto de uso)

Combobox con flechas/`aria-activedescendant`; helpers de fecha unificados en lib/format;
tarjetas móviles también en la ficha de cliente; `scope="col"` en tablas; unificar los ~6 spinners
manuales restantes; card del plan Gratis + CTA contacto Enterprise en precios; twitter card;
countdown en deadline de pago; testimonios reales (bloqueado en input del dueño).

### Cierre del backlog cosmético (2026-07-22, commit `534eb83`)

Ejecutado todo lo accionable de la lista de arriba: `scope="col"` en las 49 cabeceras de las 9
tablas; combobox de cliente con flechas/Home/End/Enter/Escape + `aria-activedescendant`; citas en
tarjetas en móvil dentro de la ficha de cliente; cuenta regresiva viva en el deadline de pago (roja
bajo 5 min, refresca al llegar a cero); twitter card en el layout raíz **y** metadata propia de la
vitrina (`/reservar/[slug]` compartía el OG del SaaS en vez del de la barbería); CTA de contacto en
el plan Enterprise; y `todayYmd` / `nextDaysYmd` / `tzFormatters` / `weekdayShortFromYmd` unificados
en `lib/format` (este último dejaba de construir un `Intl.DateTimeFormat` por chip de día en cada
render).

Dos ítems quedan cerrados sin código, a propósito:

- **Card del plan Gratis**: ya existía (la franja bajo la grilla en `pricing-plans.tsx`); es
  deliberadamente una franja y no una 4ª card para que el primer precio de la grilla ancle.
- **Spinners manuales**: los 44 botones con `Loader2` ya deshabilitan mientras cargan, así que
  moverlos al prop `loading` del `Button` solo agregaría `aria-busy` a cambio de tocar 25 archivos.

Único pendiente real: testimonios reales, bloqueado en input del dueño.
